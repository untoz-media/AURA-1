# AURA-1 Training

Initial training code will use QLoRA to adapt Qwen3-4B-Base for AURA-specific behavior.

This directory is intentionally kept lightweight during project initialization. Training scripts must remain reproducible and should document hardware, software versions, dataset versions and hyperparameters for every model release.
