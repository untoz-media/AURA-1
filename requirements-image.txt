# Optional dependencies for AURA-1 local image generation
# Install only if you want the image-generation feature.

diffusers>=0.35,<1
transformers>=4.55,<6
accelerate>=1.10,<2
safetensors>=0.5,<1
Pillow>=11,<12
# Install the PyTorch build appropriate for your NVIDIA/CUDA setup separately.
